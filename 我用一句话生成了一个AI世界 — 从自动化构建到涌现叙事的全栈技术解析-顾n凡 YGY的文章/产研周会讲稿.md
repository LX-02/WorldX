# WorldX 产研周会技术分享讲稿

> 预估时长：25–35 分钟
> 配合页面 http://localhost:3200/share 使用，按页面滚动顺序讲解

---

## 开场（首屏）

大家好，今天给大家分享一个开源项目叫 WorldX。

一句话介绍它做了什么事：**你输入一句自然语言描述，它几分钟之内帮你自动生成一个可运行的 AI 世界**——包括地图、角色、交互规则，然后角色会在这个世界里自主生活、社交、对话，涌现出没有人提前写过剧本的故事。

这个项目的作者叫顾n凡（GitHub ID: YGYOOO），他在 10 天婚假里用 vibe coding 的方式爆肝做出来的。项目今年 4 月底开源，同步发了一篇很详细的技术文章。今天分享的内容主要就来自这篇文章和项目本身。

我今天做的事情更像一个搬运工——把作者在文章里记录的思考过程、实验结果和发现整理出来讲给大家。为什么要搬运这些内容？因为作者在实现过程中实实在在地触碰到了当前模型的能力边界——哪些事模型能做好、哪些事做不好、边界在哪里——这对我们理解和使用模型很有参考价值。而且作者绕过这些边界的思路也很有启发，不是硬堆 prompt 或换更强的模型，而是重新定义问题。

[xxx演示项目，介绍功能xxx]

---

## 一、先演示，再拆解（#demo）

> 【操作：切到 WorldX 本地运行的页面，使用内置世界演示】

我先不讲架构，直接让大家看看这个世界跑起来是什么样的。

（演示要点）
- 打开首页，选一个内置世界，点播放。
- 指出地图上角色在自主移动、对话。
- 展开侧栏，看角色详情：人设、记忆、情绪状态。
- 点开事件流，看角色做了什么决策、和谁聊了天。
- 如果时间允许，展示上帝模式：广播一条事件，观察角色反应。

要注意的几点：
1. 这不是一个普通的地图编辑器，也不是一个套了游戏皮肤的聊天机器人。
2. 这里的地图是可走的，区域是有语义的，角色是有状态的，事件会沉淀成记忆。
3. 这些行为不是脚本播放，而是每个角色反复经过"感知-决策-执行"循环后的结果。

大家看到的这个世界，从地图到角色到交互，都是从一句话自动生成的。接下来我讲讲它怎么做到的。

---

## 二、为什么这个项目有意思（#why）

> 【页面滚动到对比表格】

要理解 WorldX 的价值，需要先知道它之前的"AI 小镇"是什么样的。

2023 年斯坦福发了一篇很火的论文叫 Generative Agents，证明了一个很有趣的方向：把 AI 角色放进一个小镇里，让它们自主生活、社交、记忆，能涌现出类似真人社会的行为。后来沿着这个方向出了很多项目，比如 ai-town、Microverse。

但这些项目有一个共同的瓶颈：**世界是固定的**。地图需要人工搭建，角色需要预设，想换一个场景基本得从头来。

作者想解决的就是这个问题：**不只是让 AI 角色在世界里自动行动，而是让"创造世界"这件事本身也自动化。**

大家看这个对比表：

| 维度 | 斯坦福 Generative Agents | WorldX |
|------|------------------------|--------|
| 世界 | 手工搭建的固定小镇 | 一句话自动生成 |
| 地图 | 预制 Tiled 地图 | 多模态管线自动生成 + 自我审查 |
| 角色 | 预设固定角色 | AI 自动设计 + 生成角色样貌和动画 |
| 自我修正 | 无 | 全链路审查 → 约束注入 → 重试循环 |

简单来说，Generative Agents 证明了"AI 角色可以在固定世界里涌现社会行为"，WorldX 进一步回答了"世界本身也能自动生成"。

---

## 三、整体架构（#architecture）

> 【页面滚动到架构图】

WorldX 由四个模块组成，分别用不同类型的模型。

1. **Orchestrator（编排器）**：接收用户一句话，调用强推理模型，输出一份叫 `world-design.json` 的结构化蓝图。这是全系统的合约——后续的地图生成、角色生成、运行时模拟全部从这一份 JSON 里消费信息。作者的设计思路是：不让各个环节各自发挥，而是先用一个结构化方案把所有后续模块约束住。

    当用户输入一句话后，系统先让 LLM 扮演"世界建筑师"，输出结构化蓝图。这里不仅是扩写设定，还包含很多工程边界：
   - 世界名称、描述、语言。
   - 地图描述和构图规划。
   - 场景类型：开放场景或封闭场景。
   - 时间配置：起止时间、显示格式。
   - 功能区和交互元素：最多 8 个。
   - 角色：最多 8 个。
   - 全局动作和局部交互。

2. **Generators（生成器）**：根据蓝图生成地图和角色素材。这是挑战最大的部分，也是今天要重点讲的。

3. **Server（模拟引擎）+ Client（游戏客户端）**：Server 驱动运行时角色行为——决策、对话、记忆、反思；Client 用 Phaser 3 + React 渲染世界。

这里面最关键的一个设计是 `world-design.json` 扮演的角色。它不是一份说明文档，而是一份合约——一次 LLM 调用生成的数据，从世界设计一直用到运行时模拟，不需要人工编辑。这让整个管线有了一致性。


我今天做的事情更像一个搬运工——把作者在文章里记录的思考过程、实验结果和发现整理出来讲给大家。为什么要搬运这些内容？因为作者在实现过程中实实在在地触碰到了当前模型的能力边界——哪些事模型能做好、哪些事做不好、边界在哪里——这对我们理解和使用模型很有参考价值。而且作者绕过这些边界的思路也很有启发，不是硬堆 prompt 或换更强的模型，而是重新定义问题。

---

## 四、地图生成——最难的部分（#map）

> 【页面滚动到地图章节】

接下来进入今天的重头戏。

地图生成是最值得展开的部分，因为它集中体现了作者的工程思考：面对模型不稳定和坐标不精确，不是硬让 LLM 做它不擅长的事，而是重构问题。

首先要明确一点：**WorldX 需要的不是一张好看的背景图，而是一张能被代码理解、能被角色导航和交互的地图。** 最终产物要同时包含：美术、语义、精确坐标、可行走网格、以及一份 Tiled JSON 文件让游戏引擎直接加载。

这就把问题难度拉高了很多。作者在文章里列出了他考虑过的两条路线。

### 路线一：文生图直接生成

> 【展示 images/006.jpg】

这是最自然的第一反应——既然文生图模型已经很强了，直接生成一张游戏地图似乎可行。

作者在文章中总结了这条路线的优缺点：

优点很明显——美术效果好，风格自由度高，不依赖固定素材库。

但问题也一大堆——很难保证所有道路连通，容易出现人物、车辆等动态元素，视角可能不稳定（透视/等距/俯视混杂），遮挡关系不可控，建筑内部不可见时角色无法进入。而最关键的是：**生成的只是一张图片，没有区域、坐标、碰撞和语义。** 它不是一张游戏地图，只是一张画。

> 【展示 images/007.jpg 问题清单】

### 路线二：素材拼装

> 【展示 images/009.jpg】

另一个方向是传统游戏开发的做法：用素材块拼装地图。学术界也有人做过，比如盛大 AI 研究院的 World Craft 论文。

作者分析过这条路线：优点是坐标和结构天然明确，可以直接生成游戏地图文件，可插入碰撞、导航、交互信息。但问题也很突出——素材库限制风格导致地图容易趋同，

LLM 直接输出复杂布局坐标不稳定，要让 AI 理解每个 tile 的视觉含义很难，而且相关论文路线通常还需要微调和固定素材库，工程成本很高。

作者在文章中提出了一个很好的类比：**为什么 AI 写网页很强，但 AI 拼游戏地图很难？** 他认为原因是 HTML/CSS 的元素结构和视觉效果之间的关系是清晰、确定的，而每个素材块到底"长什么样"、拼起来是否自然，模型并没有稳定的内部表示。

### 最终选择：文生图 + 约束 + 审查

> 【展示 images/010.jpg 审查流程图】

作者最终回到了文生图路线，因为它最符合"一句话生成任意风格世界"的目标。但他加入了两个关键机制。

第一个是 **prompt 约束**。作者列出了一组明确的生成约束：道路互相连通、纯背景不出现动态元素、接近俯视视角、不允许严重遮挡主要路面、可进入建筑采用横切视角能直接看到内部结构。

但是由于模型存在一定的不稳定，并且地图自由程度高，没有办法一次性将所有的约束都给出。

第二个是**自我审查循环**。因为即使是目前最强的文生图模型如 nano banana 2 或 GPT Image 2，也无法确保一次性所有规则都准确生成。所以作者引入了视觉审查模型来 review 生成结果——不通过的话，把反馈转成新约束，**追加**到下一轮 prompt。

这里作者特别强调了一个关键细节：是追加，不是替换。约束是累积的，每一轮生成都带着之前所有的教训，所以这个过程是逐步收敛的。

这解决了视角、连通性、纯背景这些相对容易约束的问题。但还有一个更难的问题没解决——

---

## 五、地图标注——核心突破（#breakthrough）

> 【页面滚动到 breakthrough 章节，深色背景】

**怎么让一张 AI 生成的图片变成有语义、有坐标、能被代码理解的地图？** 这是整个项目最难的部分，也是作者文章中着墨最多的地方。

需要标注三类东西：
- 可行走区域（角色能踩的地方）
- 可进入建筑/可交互区域（比如杂货店、咖啡厅）
- 可交互元素（比如喷泉、咖啡机）

作者在文章里记录了他尝试过的三种思路，我按顺序讲一下他的实验过程和发现。

### 思路一：多模态大模型直接标坐标

> 【展示 images/011.jpg 和 images/012.jpg】

最直接的想法是让多模态大模型看图报坐标。作者做了实验——让 Gemini 3.1 Pro 识别图中的杂货店、咖啡厅、菜园，返回每个区域的左上角和右下角坐标。

结果是：模型确实知道这些东西大致在哪，但坐标精度远远不够。

作者说这其实是预期中的结果——**多模态大模型被训练出来是为了像人一样理解图片内容，不是为了精确读像素坐标的。** 人也能看出"咖啡厅在左上角"，但你让任何人肉眼报精确坐标都不可能。


### 思路二：网格辅助 + 自我审查

> 【展示 images/013.jpg 和 images/015.jpg】

既然模型"没有尺子"，那就给它一把——作者尝试给图片打上坐标网格，每个网格交叉点都是具体坐标值，让模型有参考。然后加上审查循环：标完坐标画回图上，让模型审查对不对，不对就调整。

作者的实验结果是：准确性确实有提升，但有两个问题：
1. 为了限制 token 消耗，他设置了审查上限 5 次，但 5 次仍然很难稳定准确——这还是比较简单的地图，复杂地图所需的次数难以估计，token 和时间成本都不可接受。
2. 对大范围、不规则的可行走区域几乎无解——真实游戏地图的可行走区域不是几个矩形框能表示的，需要几百上千个小方块逐格判断。

这里插一句，之前有同事分享过一个类似的 case：给图片打上辅助框之后，大模型反而理解不好原来的图片内容了——额外的标注信息会干扰模型对原图的理解。网格辅助本质上也是这个问题：你加的"尺子"越多，模型要同时处理的视觉信息越复杂，原本能看懂的东西反而可能看不准了。

不过作者也指出，这个方案对于标注建筑、可交互元素等倒是理论上可以做到"勉强可用"，但有一类标注用这种方案几乎不可解——可行走区域标注，也就是大范围的、不规则区域的标注。

到这里似乎走入了死胡同。作者在文章里说，他甚至问了所有顶尖模型，给出的全是错误的方案。

### 关键转折：作者换了一个视角思考

> 【这里语速放慢，这是全场最核心的部分】

接下来是作者文章里最精彩的一段。他从模型的视角中抽离出来，重新站在"人"的角度想这个问题。

如果让你标注一张地图的可行走区域，你会怎么做？一种方法是拿尺子一点点量坐标——但这就回到了前面的困境。

但人有一种更自然的做法：**拿笔在图上把能走的地方涂出来**——作者原话说"这连小孩子都擅长"。你可能不知道精确坐标，但你一眼就能看出哪里能走、哪里不能走。涂完之后，程序比较原图和涂色图的色差，就能反推出所有坐标。

然后作者想到：这个思路能迁移到大模型上吗？大模型和人一样，能相对清楚地识别出"什么地方可走、什么地方不可走"，只是不知道精确坐标而已——那能不能让它"在原图的基础上涂抹"呢？

### 最终方案：图像模型叠加标注 + 色差定位

> 【展示 images/016.jpg 和 images/017.jpg，这是效果最震撼的对比】

大家看作者的实验结果。左边是一张结构错综复杂的地图，可行走区域远不止主路，用多模态模型标注坐标几乎不可能。

右边是作者把这张图传给文生图模型（nanobanana），让它把所有可行走区域涂出来，给定好色值、透明度后的结果——**所有可行走区域被极其准确地标注了出来。**

流程是：
1. 把原图传给图像编辑模型，让它用指定颜色和透明度标出目标区域。
2. 得到标注图后，程序对比原图和标注图的色差。
3. 根据色差提取坐标和可行走网格。

作者在文章中总结了这个方案的本质——一次**任务重构**：
- AI 不擅长精确报坐标。
- AI 擅长理解哪里是路、哪里是建筑、哪里是物件。
- CV 算法擅长从像素差异里算精确坐标。

最终变成：**AI 做视觉判断，程序做精确计算。** 用作者的话说：**生成式 AI 的不确定性输出，被转化成了确定性的 CV 计算。**

### 核心洞察：图像生成即视觉理解

> 【页面滚动到大字洞察块，停顿几秒让大家看】

这是作者从这个过程中提炼出来的核心发现，也是我觉得整篇文章里最有启发性的一句话：

**图像生成即视觉理解。**

作者的原话是：图像生成模型对图像内容的理解能力可能远超人们通常认为的，**因为不理解就根本无法生成**。

我们通常把图像生成模型理解为"画图工具"。但作者指出，如果它能按照语义准确涂出可行走区域，说明它不只是在生成像素——它理解了图像中的空间、物体、材质和可通行性。

作者还做了一个延伸：类似于文本是语言任务的输出格式，图像本身也可以作为视觉识别和分析任务的输出格式。常规大模型是"写"出答案，图像生成模型是"画"出答案——而且在很多视觉任务中，"画"出来的效果远超"写"出来的。

有意思的是，Google DeepMind 在 4 月 29 号发了一篇论文，标题就叫"Image Generators are Generalist Vision Learners"，和作者这里的思路不谋而合。作者自己也提到了这个巧合——他的开源时间还更早一点。

> 【如果听众是做 AI 产品的，可以加这一段】

这个发现对我们做产品也有启发：**不要只问"模型能不能直接给我要的结构化答案"，还可以问"能不能让模型用它最擅长的模态表达答案，再由程序把答案转成结构化结果"。**

### 补充：多色彩划分

> 【展示 images/018.jpg 和 images/019.jpg】

可行走区域只需要一种颜色标注，但功能区和交互元素还需要知道对应关系——哪个坐标是喷泉、哪个是咖啡机。

作者的做法是给不同元素分配不同颜色，让模型用不同颜色标注，程序按颜色提取。为了稳定性，每次最多标注 4 个元素，分批进行。

---

## 六、角色动画生成（#sprite）

> 【页面滚动到动画章节】

讲完地图，再简单讲讲角色动画。这部分作者在文章里也记录了他的方案探索过程。

角色不只是要有四向图，还要能在地图上走起来——你不能让角色在地图上滑行或者跳跃代替走路。

### 三个方案的演进

**思路一：首尾帧 + 视频模型。** 用文生图生成首尾帧，视频模型补中间帧。作者认为这条路可行，但考虑到稳定性、多角色一致性、审查难度和视频模型成本，加上他当时只有 10 天时间，没有优先深入这条路线。

**思路二：直接生成分帧精灵图。** 让模型一次生成四方向行走动画的分帧图。能接入游戏引擎的 sprite sheet 机制，但每帧位置、尺寸、动作幅度难以稳定对齐。

> 【展示 images/020.jpg】

**最终方案：参考模板 + 改绘。** 作者说这其实是地图标注那里获得的经验的复用——凭空生成很难稳定，而基于参考图做改绘可以获得更强的像素级约束。

做法是提前准备一张完美的参考模板，每一帧的动作、位置、尺寸都固定好，然后让图像编辑模型在这个模板上改绘为目标角色。效果稳定到作者直接去掉了自我审查环节，省 token。

这和地图标注形成了呼应，作者在文章里也总结了这个规律：**给图像模型一个强参考，让它在约束内改绘，而不是从零自由发挥。**

---

## 七、让世界更可信的小巧思（#details）

> 【页面滚动到巧思网格】

最后快速过几个作者在项目中提到的设计细节，都是让这个 AI 世界更可信的小巧思。

**可行性守门。** 

超出能力时返回不可行，而不是偷偷合并或删减。

系统不是来者不拒的。超出 8 角色或 8 区域+元素的上限时，直接返回"不可行"，给出原因和建议——不偷偷缩水。作者的设计理念是：生成式产品不一定要假装无所不能，把边界讲清楚反而更诚实。

**IP 角色反脸谱化。** 如果你说"把孙悟空放进咖啡馆"，系统会保存他的标志性特征和心结。但作者加了一个关键约束：IP 角色 90% 的时间应该像普通人一样在新世界生活，标志性一面只有在真正被触发时才显露。这避免了 AI 角色变成"口头禅复读机"。

**角色锚定（anchor）。** 掌柜应该守在当铺，摊贩应该在摊位旁，不能到处乱跑。更巧妙的是，当你走到一个摊位前选"买炊饼"，触发的不是和摊位的交互，而是和摊主的对话——物件交互变成了社会交互，世界更像一个有人生活的地方。

**记忆与反思。** 作者在文章里有一个很好的观点：**遗忘不是 bug，是 feature。** 他认为人类的记忆也会衰减，适当的遗忘是记忆迭代、重组、压缩和泛化的前提。所以角色的记忆有衰减、淘汰和巩固机制——经常被回忆的、情感强烈的记忆会巩固为长期记忆，其余的逐渐淡忘。记忆检索没有用 embedding，而是四维加权评分（相关性、时效性、重要性、情感强度），作者的考量是：在 Agent 系统里，可调试性比精度更重要——角色总忘重要的事就调高 importance 权重，总沉浸过去就调高 recency 权重。

反思分两级：每小时的微反思从近期记忆中提炼洞察，调整关注焦点；日终深度反思回顾全天经历，强化相关记忆。反思不只是生成一段总结文本，它会反过来改变角色后续的行为和记忆结构。

**情绪系统。** 情绪不是简单的 happy/sad 标签，而是效价（Valence）+ 唤醒度（Arousal）两个维度。高效价 + 高唤醒 = 兴奋，低效价 + 高唤醒 = 愤怒。每个 Tick 情绪自然衰减。而且角色不是"情绪透明人"——只有情绪波动超过阈值时，其他角色才能感知到，让"看出别人不对劲"成为一个事件而不是常态。

**感知不是全知。** 角色做决策前只看到它应该能看到的：当前地点、附近的人、近期事件、自己最近的行为。这让"小偷看到捕快在东边，于是往西边溜"成为可能——如果角色全知，就不会有这种涌现行为了。

---

## 八、总结（Takeaways）

> 【页面滚动到最后的总结块】

最后总结一下，从作者的这个项目和文章中，我觉得有几个启发可以带走：

1. **把生成式能力放进可收敛的工程闭环。** 单次生成不可靠没关系，约束、审查、重试这些工程手段可以让不稳定的输出逐步收敛。

2. **不要强迫模型输出它不擅长的格式。** 多模态模型不擅长报精确坐标，强行让它做只会又贵又不准。

3. **让模型用最擅长的模态表达，再用程序转结构。** 这是今天最核心的一个思路——作者发现图像模型擅长画，就让它画答案；程序擅长算，就让程序从画里提取结构化数据。

4. **涌现叙事不是只靠 LLM。** 它是世界结构、记忆系统、有限感知、情绪模型、社会交互这些机制共同作用的结果。

以上就是今天的分享。这个项目是开源的，大家感兴趣可以自己跑起来试试——配好 API Key 就行。有什么问题可以交流。

---

## 备用：可能被问到的问题

**Q: 生成一个世界大概要多少 token / 多少钱？**
A: 作者提到大约几万 token，几分钟完成。具体费用取决于使用哪家模型——Google AI Studio 有免费额度。

**Q: 为什么记忆不用 embedding？**
A: 作者的判断是在 Agent 系统里可调试性比精度更重要。权重透明，改完能马上验证。向量数据库在这个场景下反而是黑盒。不过作者在未来展望里也提到了后续可能引入 embedding 检索。

**Q: 这个项目可以用在什么场景？**
A: 作者列了几个方向：视觉小说自动生成、游戏开发素材提效、AI 社交模拟研究、以及用户可以附身角色参与世界的"身临其境"模式。

**Q: 地图标注色差定位的精度如何保证？**
A: 作者在代码里做了很多细节优化——色偏匹配度、噪声过滤、窄走廊抢救、孤岛清理、边缘内缩 3%。另外外层还有视觉审查闭环，把算出来的坐标画回图上再让模型审查。

**Q: 为什么用 SQLite 而不是 MySQL/PostgreSQL？**
A: 每条时间线一个独立数据库文件，部署简单，快照就是拷贝文件。项目定位是个人可运行的开源工具，SQLite 完全够用。
